Django>=5.0,<6.0 #Django 5.0 is the latest stable release of Django, which is a high-level Python web framework that encourages rapid development and clean, pragmatic design. The version constraint "<6.0" ensures compatibility with future releases of Django while preventing potential breaking changes that may occur in major version updates.
djangorestframework>=3.15 #Django REST framework (DRF) is a powerful and flexible toolkit for building Web APIs in Django. The version constraint ">=3.15" ensures that the project uses a version of DRF that includes the latest features and improvements while maintaining compatibility with Django 5.0.
djangorestframework-simplejwt>=5.3 #djangorestframework-simplejwt is a JSON Web Token authentication plugin for Django REST framework. The version constraint ">=5.3" ensures that the project uses a version of the plugin that includes the latest features and improvements while maintaining compatibility with Django 5.0 and DRF 3.15.
psycopg2-binary>=2.9 #psycopg2-binary is a PostgreSQL database adapter for Python. The version constraint ">=2.9" ensures that the project uses a version of the adapter that includes the latest features and improvements while maintaining compatibility with Django 5.0 and DRF 3.15.
python-dotenv>=1.0 #python-dotenv is a Python library that allows you to read key-value pairs from a .env file and set them as environment variables. The version constraint ">=1.0" ensures that the project uses a version of the library that includes the latest features and improvements while maintaining compatibility with Django 5.0 and DRF 3.15.
django-cors-headers>=4.3 #django-cors-headers is a Django application for handling Cross-Origin Resource Sharing (CORS) headers. The version constraint ">=4.3" ensures that the project uses a version of the library that includes the latest features and improvements while maintaining compatibility with Django 5.0 and DRF 3.15.
drf-spectacular>=0.27 #drf-spectacular is a library for generating OpenAPI 3.0 schemas from Django REST framework code. The version constraint ">=0.27" ensures that the project uses a version of the library that includes the latest features and improvements while maintaining compatibility with Django 5.0 and DRF 3.15.
channels>=4.0 #Django Channels is an extension to Django that enables handling of WebSockets, long-poll HTTP, and other asynchronous protocols. The version constraint ">=4.0" ensures that the project uses a version of Channels that includes the latest features and improvements while maintaining compatibility with Django 5.0 and DRF 3.15.
daphne>=4.0 #Daphne is an HTTP, HTTP2, and WebSocket protocol server for ASGI and ASGI-HTTP, developed as part of the Django Channels project. The version constraint ">=4.0" ensures that the project uses a version of Daphne that includes the latest features and improvements while maintaining compatibility with Django 5.0 and DRF 3.15.
