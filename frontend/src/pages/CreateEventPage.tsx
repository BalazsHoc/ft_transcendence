import {useNavigate} from "react-router-dom";import {useTranslation} from "react-i18next";import {EventForm} from "../components/events/EventForm";import {createEvent,EventPayload} from "../api/eventsApi";export function CreateEventPage(){const {t}=useTranslation();const navigate=useNavigate();async function submit(payload:EventPayload){const event=await createEvent(payload);navigate(`/events/${event.id}`)}return <><h1>{t("createEvent.title")}</h1><EventForm onSubmit={submit}/></>}
